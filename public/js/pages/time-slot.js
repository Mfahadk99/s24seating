// Time Slot Management JavaScript

document.addEventListener('DOMContentLoaded', function () {
    // This file contains the JavaScript for the time slot management page
    // The inline script from time-slot.ejs has been moved here for better organization
    
    console.log('Time slot management script loaded');
    
    // All the functions are already defined inline in the time-slot.ejs file
    // This file exists to prevent 404 errors when the browser tries to load it
    
    // If you want to move functionality from the inline script to this external file,
    // you would need to move the functions here and reference them from the EJS file
}); 