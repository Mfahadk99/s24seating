<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Turn Times Modal</title>
  <style>
    .tt-modal-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 2000;
    }
    .tt-modal {
      background: white;
      border-radius: 8px;
      width: 90%;
      max-width: 450px;
      max-height: 90vh;
      overflow-y: auto;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    }
    .tt-modal-header {
      padding: 24px 24px 16px;
      border-bottom: 1px solid #e5e7eb;
    }
    .tt-modal-title {
      font-size: 18px;
      font-weight: 600;
      color: #111827;
      margin-bottom: 4px;
    }
    .tt-modal-subtitle {
      font-size: 14px;
      color: #6b7280;
      line-height: 1.5;
    }
    .tt-modal-content {
      padding: 20px 24px;
    }
    .tt-turn-time-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 12px 0;
      border-bottom: 1px solid #f3f4f6;
    }
    .tt-turn-time-row:last-child {
      border-bottom: none;
    }
    .tt-guest-count {
      font-size: 14px;
      color: #374151;
      font-weight: 500;
      min-width: 80px;
    }
    .tt-time-controls {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .tt-time-button {
      width: 32px;
      height: 32px;
      border: 1px solid #d1d5db;
      background: white;
      border-radius: 4px;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 16px;
      color: #6b7280;
      transition: all 0.2s;
    }
    .tt-time-button:hover {
      background: #f9fafb;
      border-color: #9ca3af;
    }
    .tt-time-button:active {
      background: #f3f4f6;
    }
    .tt-time-display {
      min-width: 80px;
      text-align: center;
      font-size: 14px;
      color: #111827;
      font-weight: 500;
    }
    .tt-modal-actions {
      padding: 20px 24px;
      border-top: 1px solid #e5e7eb;
      display: flex;
      gap: 12px;
      justify-content: flex-end;
    }
    .btn {
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.2s;
      border: 1px solid transparent;
    }
    .btn-secondary {
      background: #f9fafb;
      color: #374151;
      border-color: #d1d5db;
    }
    .btn-secondary:hover {
      background: #f3f4f6;
    }
    .btn-primary {
      background: #3b82f6;
      color: white;
    }
    .btn-primary:hover {
      background: #2563eb;
    }
    .demo-trigger {
      position: fixed;
      top: 20px;
      left: 20px;
      padding: 10px 20px;
      background: #3b82f6;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
    }
    .demo-trigger:hover {
      background: #2563eb;
    }
    .hidden {
      display: none;
    }
    @media (max-width: 480px) {
      .tt-modal {
        width: 95%;
        margin: 10px;
      }
      .tt-modal-header,
      .tt-modal-content,
      .tt-modal-actions {
        padding-left: 16px;
        padding-right: 16px;
      }
      .tt-time-controls {
        gap: 6px;
      }
      .tt-time-display {
        min-width: 70px;
        font-size: 13px;
      }
    }
  </style>
</head>
<body>
  <!-- Modal -->
  <div id="modalOverlay" class="tt-modal-overlay hidden">
    <div class="tt-modal">
      <div class="tt-modal-header">
        <h2 class="tt-modal-title">Turn Times</h2>
        <p class="tt-modal-subtitle">
          Specify the average turn time based on party size. View how effective your settings are in the turn times report.
        </p>
      </div>
      <div class="tt-modal-content">
        <!-- Turn time rows will be injected here by external logic -->
      </div>
      <div class="tt-modal-actions">
        <button class="btn btn-secondary" id="cancelTurnTimesBtn">Cancel</button>
        <button class="btn btn-primary" id="saveTurnTimesBtn">Save</button>
      </div>
    </div>
  </div>
</body>
</html>
